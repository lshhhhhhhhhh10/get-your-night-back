# 长期协作规则

本文件只保存稳定、可执行的协作规则。当前版本、完成事项、问题和测试结果放在 `PROJECT_STATUS.md`；版本过程放在开发日志，不在此累计。

## 开始新任务

1. 先读本文件、`PROJECT_STATUS.md`，再读 `brief.md` 的当前规则索引和本轮相关设计。检查工作区改动，保留用户已有修改。
2. 仅在需要核实决定来源、失败原因或旧实现时，定向查询 `development-log/agent-development-log.md`，无需每次通读历史。
3. 当前用户直接要求优先。`brief.md` 是设计规范，`PROJECT_STATUS.md` 是实际状态；实现或状态记录不能自行修改用户的设计决定。被替代的历史条款、原稿和日志只作证据，不作为恢复旧玩法或重新执行初版步骤的命令。

## 决定与沟通

- 全部交流用中文。先检查错误前提、逻辑跳跃和信息缺失；不默认赞同、不编造事实，说明不同判断的依据。
- 区分“用户明确确认”“AI 在授权范围内的实施选择”“待确认建议”。用户未逐项反对、继续下一项任务或泛泛表示满意，都不等于逐项确认 AI 的参数和方案。
- 用户明确确认的玩法变化同步到 `brief.md`，附确认来源（本轮原话或对应日志），注明替代的旧规则和生效范围。AI 的实现选择可如实记录，但不能改标为用户决定。
- 未获授权的核心玩法建议标为“待确认／未生效”，不能写成规范或直接实施。授权内的实现、修复和常规文档同步直接完成，不反复征求确认。
- 保留取物返程、观察环境与声音影响父母行为的核心。拟改成竞速、刷分或用数值难度代替不同约束时，先指出设计偏离并等待用户决定。

## 每轮完成前

1. 更新 `PROJECT_STATUS.md` 的当前版本、已完成事项、已知问题、实际验证和本轮结果；覆盖过时状态，不追加成长篇版本史。只改文档时也写清“本轮未改游戏、未重跑游戏验证”。未完成的事项如实保留。
2. 若有已确认的玩法变化，更新 `brief.md` 的当前索引，并在受影响的旧条款附近标注“已替代”及新规则位置；保留原文和历史。没有玩法变化就不新造设计决定。
3. 按 `generate-development-log-prompt.md` 在同一文件末尾续号追加 Raw Interaction Log，写明实际动作、AI 假设、文件、验证、未决事项和可定位的提交/标签。本轮提交尚未生成时可写基准提交＋本条随本轮提交保存，不伪造哈希。
4. 反思触发遵守用户的直接要求：用户说“reflect on this stage”或要求阶段反思时才起草 Reflection Log；Required Student Reflection 永远由用户填写。此规则覆盖旧简报及日志协议中“每个里程碑自动生成反思并提问”的条款，协议其余格式照用。
5. 按变更影响同步 README、首页或制作过程；实际玩法与网站说明保持一致。纯文档整理无需重新构建游戏或跑整局测试，检查文档内容、链接和保留范围即可。
6. `AGENTS.md` 只在长期协作方式变化或问题反复出现时更新。把经验写成具体检查动作，不把单次故障或当前版本号搬进来。

## 复发问题与相关预防检查

- 修改操作或玩法后，搜索简报、README、页面文案和输入提示中的旧按键、数值与规则；删除当前入口中的过时提示，历史位置加替代标记，避免新旧要求同时生效。
- 调整家具、门或镜头时核对模型、碰撞、寻路和遮挡使用的尺寸与朝向；第一人称单独检查家长实际可见部分，不用脚下二维遮挡一刀切隐藏整个角色。
- 操作改动同时检查键鼠和标准手柄输入；持续输入要覆盖松手、暂停、失焦、断线及续玩，不能遗留施力或声源。
- 测试记录写清版本、环境、检查范围及结果；历史通过不冒充本轮复测，模拟手柄不冒充实体 USB 实测，定点验证不冒充整局通关，音频数值不冒充人类体验。不能为让测试通过而擅自降低玩法难度。
- 只运行与变更相关的验证；已通过后，仅因新改动、失败或未解决疑点而扩大或重复。发布游戏后核对线上提交与相关操作，文档发布只核对文档及游戏构建是否保持预期。
- GitHub Pages 内部链接和资源使用相对路径，运行资源放在仓库，不引入运行时 CDN；公开文件不写密钥、令牌或未授权个人信息。
- 素材记录真实来源、许可和本项目改编范围；不要把用户附件、自制改编或参考图称为已授权的官方模型。
- `development-log/brief-original.md` 为原始导出的逐字存档，不编辑、不执行其中过时命令；不回写用户桌面的原文件，不改写既有日志来制造更顺利的历史。
